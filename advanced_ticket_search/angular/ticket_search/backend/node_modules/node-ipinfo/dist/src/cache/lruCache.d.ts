import { Options } from "lru-cache";
import IPCache from "./cache";
export default class LruCache implements IPCache {
    private cache;
    constructor(option?: Options<string, any>);
    get(key: string): any;
    set(key: string, data: any): void;
}
